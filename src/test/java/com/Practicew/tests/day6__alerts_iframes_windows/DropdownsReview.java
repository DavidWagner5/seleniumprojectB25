package com.Practicew.tests.day6__alerts_iframes_windows;

public class DropdownsReview {

    //#1   if dropdowns are created using anything other than <select> tage, it is a non-select dropdown
    //#2   to create select class object, locate <select> dropdown and pass it into the constructor, now i can use methods that comes from select class

    //getOptions --> to get all <options>, return type is List<WebElement>
    //getFirstSelectedOption --> returns the currently selected option as a single web element
    //selectByIndex, selectByValue, selectByVisibleText


}
